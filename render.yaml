services:
  - type: web
    name: dream-radio-v23-custom-story-generator
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
